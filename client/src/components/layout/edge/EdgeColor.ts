const style = {
  strokeWidth: 4,
  stroke: '#000000', // white color
};
const BlackStyle = {
  strokeWidth: 4,
  stroke: '#000000', // Black color
};

const RedStyle = {
  strokeWidth: 4,
  stroke: '#ff0000', // Red color
};

const ElectricityStyle = {
  strokeWidth: 4,
  stroke: '#008000', // Green color
};

const SciencesStyle = {
  strokeWidth: 4,
  stroke: '#079cba', // Blue color
};

const ItStyle = {
  strokeWidth: 4,
  stroke: '#ffa500', // Orange color
};
const MechatronicsStyle = {
  strokeWidth: 4,
  stroke: '#641d4b', // Orange color
};

export {
  style,
  BlackStyle,
  RedStyle,
  ElectricityStyle,
  SciencesStyle,
  ItStyle,
  MechatronicsStyle,
};
